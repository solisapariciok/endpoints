body {
  font-family: Arial, sans-serif;
  background: #f2f6ff;
  text-align: center;
  padding: 40px;
}

#app {
  background: white;
  padding: 20px;
  border-radius: 10px;
  display: inline-block;
  box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

p {
  font-size: 20px;
}
